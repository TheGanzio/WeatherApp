import React from "react";

const Info = props => (
        <div className="info">
            <h2 className="textSite">Weather App</h2>
            <p className="textSite">Find out the weather in your city</p>
        </div>
    );

export default Info;